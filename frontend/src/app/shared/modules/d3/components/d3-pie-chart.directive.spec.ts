import { D3PieChartDirective } from './d3-pie-chart.directive';

describe('D3PieChartDirective', () => {
  it('should create an instance', () => {
    const directive = new D3PieChartDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
