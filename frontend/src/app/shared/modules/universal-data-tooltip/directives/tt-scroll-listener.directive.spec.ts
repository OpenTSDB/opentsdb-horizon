import { TtScrollListenerDirective } from './tt-scroll-listener.directive';

describe('TtScrollListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new TtScrollListenerDirective();
    expect(directive).toBeTruthy();
  });
});
