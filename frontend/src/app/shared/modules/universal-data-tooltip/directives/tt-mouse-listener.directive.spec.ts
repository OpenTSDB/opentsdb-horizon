import { TtMouseListenerDirective } from './tt-mouse-listener.directive';

describe('TtMouseListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new TtMouseListenerDirective();
    expect(directive).toBeTruthy();
  });
});
